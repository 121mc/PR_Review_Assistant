## 哪些 Superpowers 技能在你的项目里发挥了最大作用？哪些感觉是"形式大于实质"？

- `writing-plans` 很有用，它把一个大的工作拆成了很多个模块，并基于 `subagent-driven-development` 可以提升智能体开发的准确率和代码可用性。
- `using-git-worktrees` 也很有帮助，它帮我管理不同子智能体的产物，避免出现混乱，最后将所有的子智能体的 worktree 合并到一起就是一个完整可用的产物，非常地简洁高效。
- `test-driven-development` 虽然感觉非常地费 token ，每次写代码都要先写测试，体感上降低了开发效率，但是实际上这样的编程方式还是很好的，如果不写测试智能体就有可能写出莫名其妙的 bug 还不知道问题在哪，最后 debug 的时候花费的时间和 token 未必比先写测试要少。所以我认为先写测试还是很有必要的，除非某一天 AI 真的发展到不可能写出 bug 的程度。
- `brainstorming` 虽然对编写设计文档很有帮助，但是我个人感觉还是有一些问题的，比如很多问题它就是给你几个选项让你选，最终形成的设计文档给我一种被 AI 牵着鼻子走的感觉，自己的想法可能并不包含在 AI 给的几个选项里面，但是我看了这些选项就很容易被困在里面而忘记自己原本的一些想法。

## TDD 强制在 AI 协作下的体感如何？它是阻碍还是放大器？

- 其实我也尝试过让 AI 直接写代码不要准备测试，实测这样写起来真的很快，一个 task 基本只要几分钟就搞定了，所有的 task 加起来可能也就一个小时多一点，但是这么干肯定是会出问题的，比如在某些地方会出现奇怪的 bug 。
- 所以我个人认为 TDD 是挺有必要的，能显著降低代码出问题的概率，提升智能体开发过程中的稳定性。虽然真的很费 token 和时间，我这个项目整个开发过程加起来一共有 6 个小时，总共大约消耗了 1 亿多的 token ，但是最终呈现的成品是直接可用的，总体来看还是很不错的。

## subagent-driven 工作流让智能体能自主跑多久不偏题？什么样的 task 拆解颗粒度最优？

- 一共跑了 6 个小时，从只有设计和规划文档到给出完整的产物全程不需要人工干预。
- 按功能模块来拆分 task 应该是比较好的，一个功能模块交给一个子智能体去做，完整的功能模块也方便进行测试，合并起来也比较方便。如果太细测试就不太好做，接口也不是非常好设计，太粗的话 subagent-driven 工作流就没啥意义了，反正还是一个智能体做一堆东西出来。

## SPEC 与 PLAN 的质量如何影响了后续实现质量？是否有过"规约不清晰导致 subagent 跑偏"的具体案例？

- 我认为 codex 生成的 SPEC 与 PLAN 质量很高，最后实现的质量也很高，子智能体也没有跑偏，整体开发过程很顺利，没有出现什么意外。

## 你设计的最有效的 prompt / context 策略是什么？为什么有效？

- 没有什么特别的提示词和上下文设计，就是指定了每一个环节要用的 skill 的名称，并要求 AI 根据文档进行开发。我认为提示词做到清晰明确，减少 AI 自由发挥 / 随意调用不正确的 skill 就行。

## （若使用了 Open Design）它的 skill 库与设计系统在多大程度上消除了"AI 生成界面千篇一律"的问题？

- 个人感觉没有特别大的用处。生成出来的界面虽然美观、简洁、高效，但是摆脱不了那种 AI 味，这个只能说是将 AI 生成的界面从千篇一律变成了千篇十律，没有很本质的变化。

## 如果重做，你会改变哪些做法？

- 如果重做我会改变 brainstorming 的流程，我将尽可能完善我一开始给 AI 提供的内容，尽量减少被 AI 引导（误导）的情况。
- 我还会改变 Open Design 的用法，可能会把一些现有的我觉得好的设计丢给它让它模仿（虽然也不知道能不能做出令人满意的效果）


## 你对当前 AI4SE 工具与方法论的整体看法。

- AI 确实对软件工程体系造成了非常大的影响，AI 代码生成极大程度上加速了开发的进程，但是正如之前课上所说，vibe coding 产生的代码都是死的，没有人知道程序具体的逻辑和写法，因此程序无法被维护。因此 superpowers 的规约化开发为这种情况提供了一个解决方式，即用详细的 SPEC 和 PLAN 文档约束 AI 的行为，并用 TDD 、子智能体和代码评审来进行开发，我认为这样开发效率虽然远低于直接 vibe coding ，但是胜在稳定和代码的可解释性，因此是一个值得采用的方案，我现在已经将这套方法和 superpowers 的工具用于其他项目的开发。